const multer=require('multer'),path=require('path'),fs=require('fs'); const dir=path.join(__dirname,'..','uploads'); if(!fs.existsSync(dir))fs.mkdirSync(dir,{recursive:true}); const allowed=['.jpg','.jpeg','.png','.webp','.pdf']; module.exports=multer({storage:multer.diskStorage({destination:(q,f,cb)=>cb(null,dir),filename:(q,f,cb)=>cb(null,`${Date.now()}-${Math.round(Math.random()*1e9)}${path.extname(f.originalname).toLowerCase()}`)}),limits:{fileSize:5*1024*1024},fileFilter:(q,f,cb)=>allowed.includes(path.extname(f.originalname).toLowerCase())?cb(null,true):cb(new Error('Tipo de archivo no permitido'))});
